#include <stdio.h>
#include <stdlib.h>
int main (){
	float prezzo,accomp;
	int a,altezza,eta,sup,accomp
	printf("premere 1 se e' disabile premere 0 se non e' disabile");
	scanf("%d",&a); // variabile che indica se e' disabile
	if(a==1) {
		prezzo=0,00
	}
}
